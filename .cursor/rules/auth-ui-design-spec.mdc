---
description: 认证页面（邮箱登录/修改密码/注册）统一设计规范
alwaysApply: true
---

# 认证页面设计规范（来源：蓝狐设计图）

适用范围：本项目内认证相关页面与组件（邮箱登录、修改密码、注册）默认遵循本规范。

## 字体与排版

- 中文字体：`PingFang SC`
- 数字字体：`DIN Alternate`（数字优先使用）
- 一级标题：`16pt`，`semibold`，行高 `100%`
- 二级标题：`14pt`，`semibold`，行高 `100%`
- 一级正文：`14pt`，`regular`，行高 `单行100% / 多行160%`
- 二级正文：`12pt`，`regular`，行高 `单行100% / 多行160%`
- 注释：`10pt`，`regular`，行高 `100%`

## 布局与间距

- 左内边距：`16px`
- 右内边距：`16px`
- 组件纵向间距：`12px`
- 圆角：`16px`

## 颜色

- 当前图片未提供可识别的具体色值（HEX/RGB）。
- 在补充色值前，优先复用项目现有主题色与中性色，避免新增临时颜色常量。

## 开发约束

- 新增认证页面时，优先抽离通用样式常量（字体级别、间距、圆角）避免硬编码。
- 若设计稿与本规则冲突，以最新蓝狐设计稿为准，并同步更新本规则文件。
